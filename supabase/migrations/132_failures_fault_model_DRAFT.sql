-- 고장 처리등록 시 기사가 고른 기종(에러코드 검색 기준)을 별도로 저장한다.
-- 지금까지는 이 값을 화면에서만 쓰고 저장을 안 해서, 에러코드집 "과거처리이력"이
-- 호기에 등록된 실제 기종(units.model)으로만 찾았다 — 기사가 코드집에 그 기종이
-- 없어 다른(비슷한) 기종을 골라 입력한 경우(예: 호기는 STVF-2인데 코드집엔 STVF-2가
-- 없어 STVF5로 입력) 과거처리이력에 영원히 안 걸렸다. 이제 처리 등록 시 고른 기종을
-- 그대로 저장해서, 그 기종 기준으로 이력을 찾게 한다(2026-08-26).
alter table public.failures
  add column if not exists fault_model text;
