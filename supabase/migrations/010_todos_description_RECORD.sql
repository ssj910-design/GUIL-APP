-- ============================================================
-- 010. [기록용] todos.description — 이미 실운영에 적용돼 있음
--
-- 전임자가 "관리자 할일 내용 추가 기능"(커밋 c6ecad8 부근) 개발 중
-- 대시보드에서 직접 추가한 것으로 확인됨 (2026-07-16 실DB 조회로 발견).
-- 마이그레이션 이력에 남기기 위해 기록. 아래 문장은 재실행해도 안전.
-- ============================================================

alter table public.todos add column if not exists description text;
